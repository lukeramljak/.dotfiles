#!/usr/bin/env zsh

pushd $HOME/.dotfiles

git submodule update --init --recursive > /dev/null

if [[ "$(uname)" == Linux ]]; then
  stow --restow linux common
  echo "stowed all folders"
else
  stow --restow common
  echo "stowed common"
fi

popd
